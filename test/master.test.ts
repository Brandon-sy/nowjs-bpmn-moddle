import "jest";


// tslint:disable:no-empty
beforeAll(() => { });

beforeEach(() => {

});

afterAll(() => { });

afterEach(() => { });

describe("Master Test",  () => {

  it("first test",  () => {
    expect.assertions(1);
    expect(36).toEqual(36) ;
  });

});
